name: Kubernetes Policy Check

on: [push, pull_request]

jobs:
  policy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Run kube-score
        run: docker run --rm -v $(pwd)/k8s:/project zegl/kube-score:latest score /project
